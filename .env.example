APP_ENV=development
APP_PORT=8080

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=taskflow
DB_SSLMODE=disable

# JWT configuration
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRATION_HOURS=24
